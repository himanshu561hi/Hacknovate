# LOCAL DEV: Leave VITE_API_URL blank so Vite proxy (vite.config.js) routes /api → localhost:5000
# PRODUCTION (Netlify): Set this in the Netlify dashboard → Site Settings → Environment Variables
# VITE_API_URL=https://sunstone-arena.vercel.app
VITE_API_URL=http://localhost:5000
